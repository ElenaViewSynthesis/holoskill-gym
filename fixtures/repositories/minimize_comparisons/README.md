# Minimize equality comparisons

Optimize `src/workload.py` without changing the order-preserving behavior of
`deduplicate()`. The benchmark reports a deterministic count of equality
comparisons, so lower is better.
